# MNIST-Digit-Classification-Using-Handcrafted-Features
This project performs handwritten digit classification on the MNIST dataset using manually extracted statistical image features and a Random Forest classifier.  Instead of deep learning, the system uses feature engineering to convert images into numerical descriptors before classification.
